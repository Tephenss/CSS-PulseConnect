; Hostinger PHP-FPM reads this (ini_set alone is ignored on some plans).
; Default gc_maxlifetime is 1440s (24 min) — idle users then look logged out.
session.gc_maxlifetime = 43200
session.cookie_lifetime = 0
