# Deny direct HTTP access to uploaded media.
# Serve only via intentional signed/public endpoints.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Deny from all
</IfModule>
Options -Indexes
